import { Button } from './ui/button';

export default function MainNav() {
    return (
        <Button variant="ghost" className='font-bold transition-colors hover:text-orange-500 hover:bg-white'>
            Log In
        </Button>
    )
}
